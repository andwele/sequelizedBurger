// A typical (?) user.
var user = {
  first_name: "Bigfoot",
  last_name: "Smith",
  email: "bigfoot@legend.wow"
};

// Calling a non-existent (?) method.
console.log("The result of calling user.tostring is: " + user.toString());
